# Rules

Path-scoped instructions, read by both Claude Code and GitHub Copilot (VS Code).
Each file declares its scope with `paths:` glob frontmatter and stays under
50 non-blank lines (R-52). One scope per file. Run the `fcore-check` skill to
audit this folder against the conventions.
